import React, { Fragment } from 'react'
import styles from'./Header.module.scss'
const Header = () => {
    return (
        <header className={styles.header}>
            Menu-Button GameWiki sign-in
        </header>
    )
}


export default Header